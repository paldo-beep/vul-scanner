# vul-scanner
Network vulnerability scanner that audits open ports, flags risky services against CIS baselines, and generates JSON audit reports. Includes a Bash log monitor for detecting brute force and suspicious auth activity.
